package Arrays.ArrayList;

public class Ex1 {
    public static void main(String[] args) {
        String[] cores = {"amarelo", "azul", "rosa", "roxo", "branco"};
        for (int i = 0; i < cores.length ; i++) {
            System.out.println(cores[i]);
        }
        System.out.println(cores.length);
    }
}
